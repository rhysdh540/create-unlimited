package dev.rdh.createunlimited.fabric;

import com.terraformersmc.modmenu.api.ConfigScreenFactory;
import com.terraformersmc.modmenu.api.ModMenuApi;

/**
 * This class is used to provide a config screen for the mod in ModMenu.
 * It's currently unused (hence why the code is commented out) because the mod doesn't have a config screen.
 */
public class CUModMenuCompat implements ModMenuApi {
//    @Override
//    public ConfigScreenFactory<?> getModConfigScreenFactory() {
//        return parent -> /*something*/;
//    }
}
